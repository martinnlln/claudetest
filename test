<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Price Prediction Dashboard</title>
    <style>
        /* Reset and Base Styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            background: linear-gradient(135deg, #0d1b2a 0%, #1b263b 100%);
            color: #e0e1dd;
            min-height: 100vh;
            overflow-x: hidden;
        }

        /* Header */
        header {
            background: rgba(27, 38, 59, 0.9);
            backdrop-filter: blur(10px);
            padding: 1rem 2rem;
            position: fixed;
            width: 100%;
            z-index: 1000;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.3);
        }

        nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
            max-width: 1200px;
            margin: 0 auto;
        }

        .logo {
            font-size: 1.8rem;
            font-weight: 700;
            color: #00d4ff;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        nav ul {
            display: flex;
            list-style: none;
            gap: 2rem;
        }

        nav a {
            color: #e0e1dd;
            text-decoration: none;
            font-size: 1rem;
            transition: color 0.3s ease;
        }

        nav a:hover {
            color: #00d4ff;
            text-shadow: 0 0 10px rgba(0, 212, 255, 0.5);
        }

        /* Hero Section */
        .hero {
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            background: radial-gradient(circle at center, rgba(0, 212, 255, 0.2), transparent);
            padding: 2rem;
        }

        .hero-content h1 {
            font-size: 3.5rem;
            font-weight: 800;
            margin-bottom: 1rem;
            background: linear-gradient(45deg, #00d4ff, #ff007a);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
        }

        .hero-content p {
            font-size: 1.2rem;
            max-width: 600px;
            margin: 0 auto 2rem;
            line-height: 1.6;
            color: #b0b8c4;
        }

        .cta-button {
            background: #00d4ff;
            color: #0d1b2a;
            padding: 0.8rem 2rem;
            border-radius: 50px;
            text-decoration: none;
            font-weight: 600;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .cta-button:hover {
            transform: translateY(-3px);
            box-shadow: 0 5px 15px rgba(0, 212, 255, 0.5);
        }

        /* Dashboard Section */
        .dashboard {
            max-width: 1200px;
            margin: 4rem auto;
            padding: 2rem;
        }

        .dashboard h2 {
            font-size: 2.5rem;
            margin-bottom: 2rem;
            text-align: center;
            color: #00d4ff;
        }

        .input-group {
            display: flex;
            gap: 1rem;
            justify-content: center;
            margin-bottom: 2rem;
        }

        input, select {
            padding: 0.8rem;
            border-radius: 8px;
            border: none;
            background: #1b263b;
            color: #e0e1dd;
            font-size: 1rem;
            width: 200px;
        }

        button {
            padding: 0.8rem 1.5rem;
            border: none;
            border-radius: 8px;
            background: #ff007a;
            color: #e0e1dd;
            font-weight: 600;
            cursor: pointer;
            transition: background 0.3s ease;
        }

        button:hover {
            background: #d1005e;
        }

        #chart-container {
            background: rgba(27, 38, 59, 0.8);
            border-radius: 12px;
            padding: 2rem;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2);
        }

        /* Footer */
        footer {
            text-align: center;
            padding: 2rem;
            background: #0d1b2a;
            color: #b0b8c4;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .hero-content h1 {
                font-size: 2.5rem;
            }

            nav ul {
                gap: 1rem;
            }

            .input-group {
                flex-direction: column;
                align-items: center;
            }

            input, select {
                width: 100%;
                max-width: 300px;
            }
        }

        /* Animations */
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .hero-content, .dashboard {
            animation: fadeIn 1s ease-out;
        }
    </style>
</head>
<body>
    <!-- Header -->
    <header>
        <nav>
            <div class="logo">Price Predictor</div>
            <ul>
                <li><a href="#home">Home</a></li>
                <li><a href="#dashboard">Dashboard</a></li>
                <li><a href="#about">About</a></li>
                <li><a href="#contact">Contact</a></li>
            </ul>
        </nav>
    </header>

    <!-- Hero Section -->
    <section class="hero" id="home">
        <div class="hero-content">
            <h1>Predict the Future of Finance</h1>
            <p>Explore powerful price predictions with our advanced AI models. Stay ahead with real-time data and insights.</p>
            <a href="#dashboard" class="cta-button">Get Started</a>
        </div>
    </section>

    <!-- Dashboard Section -->
    <section class="dashboard" id="dashboard">
        <h2>Price Prediction Dashboard</h2>
        <div class="input-group">
            <input type="text" id="symbol" placeholder="Enter Symbol (e.g., AAPL)" required>
            <select id="model-type">
                <option value="ensemble">Ensemble</option>
                <option value="time_series">Time Series</option>
                <option value="ml">Machine Learning</option>
                <option value="dl">Deep Learning</option>
            </select>
            <button onclick="fetchPrediction()">Predict</button>
        </div>
        <div id="chart-container">
            <canvas id="price-chart"></canvas>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <p>&copy; 2025 Price Predictor. All rights reserved.</p>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script>
        // Chart.js Configuration
        let priceChart = null;

        function initializeChart(labels = [], data = []) {
            const ctx = document.getElementById('price-chart').getContext('2d');
            if (priceChart) {
                priceChart.destroy();
            }
            priceChart = new Chart(ctx, {
                type: 'line',
                data: {
                    labels: labels,
                    datasets: [{
                        label: 'Predicted Prices',
                        data: data,
                        borderColor: '#00d4ff',
                        backgroundColor: 'rgba(0, 212, 255, 0.2)',
                        fill: true,
                        tension: 0.4
                    }]
                },
                options: {
                    responsive: true,
                    scales: {
                        x: { title: { display: true, text: 'Date', color: '#e0e1dd' } },
                        y: { title: { display: true, text: 'Price', color: '#e0e1dd' } }
                    },
                    plugins: {
                        legend: { labels: { color: '#e0e1dd' } }
                    }
                }
            });
        }

        // Fetch Prediction from Backend
        async function fetchPrediction() {
            const symbol = document.getElementById('symbol').value.trim().toUpperCase();
            const modelType = document.getElementById('model-type').value;

            if (!symbol) {
                alert('Please enter a symbol');
                return;
            }

            try {
                const response = await fetch(`http://localhost:8000/api/predict/${modelType}?symbol=${symbol}&days_ahead=7`, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' }
                });

                if (!response.ok) {
                    throw new Error('Failed to fetch prediction');
                }

                const data = await response.json();
                if (data.error) {
                    throw new Error(data.error);
                }

                // Update Chart
                initializeChart(data.prediction_dates, data.predicted_prices);
            } catch (error) {
                console.error('Error:', error);
                alert('Error fetching prediction: ' + error.message);
            }
        }

        // Initialize Empty Chart
        window.onload = () => {
            initializeChart();
        };
    </script>
</body>
</html>
